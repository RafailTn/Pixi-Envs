# Install BiocManager if you don't have it
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")

# Use BiocManager to install the package
BiocManager::install("GenomeInfoDbData")
q()
